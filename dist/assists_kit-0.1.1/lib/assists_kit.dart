/// Public entry point of the package, for tooling that expects a library
/// named after the package. The analysis server itself loads `main.dart`,
/// which is the file name the plugin system requires.
library;

export 'main.dart';
